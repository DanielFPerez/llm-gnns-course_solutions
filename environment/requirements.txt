# ── Module I — core data & ML stack ──────────────────────────────────────────
numpy>=1.26,<2.0
pandas>=2.1,<3.0
matplotlib>=3.8,<4.0
seaborn>=0.13,<1.0
scikit-learn>=1.4,<2.0

# ── Module II — LLM stack ─────────────────────────────────────────────────────
torch>=2.3,<3.0              # base for transformers & sentence-transformers
transformers>=4.44,<5.0      # HuggingFace model hub + chat pipelines
sentence-transformers>=3.0,<4.0  # embedding models for RAG
faiss-cpu>=1.8,<2.0          # vector similarity search
accelerate>=0.30,<1.0        # device_map="auto" support
requests>=2.31,<3.0          # Ollama REST API

# ── Notebook runtime ──────────────────────────────────────────────────────────
jupyterlab>=4.1,<5.0
ipywidgets>=8.1,<9.0

# ── Modules III–IV (uncomment when starting Module III) ──────────────────────
# torch-geometric>=2.5,<3.0
# networkx>=3.3,<4.0
