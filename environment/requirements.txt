# ── Module I — core data & ML stack ──────────────────────────────────────────
numpy>=1.26,<2.0
pandas>=2.1,<3.0
matplotlib>=3.8,<4.0
seaborn>=0.13,<1.0
scikit-learn>=1.4,<2.0

# ── Module II — LLM stack ─────────────────────────────────────────────────────
torch>=2.3,<3.0              # shared base for all deep-learning modules
transformers>=4.44,<5.0
sentence-transformers>=3.0,<4.0
faiss-cpu>=1.8,<2.0
accelerate>=0.30,<1.0
requests>=2.31,<3.0          # Ollama REST API
datasets>=2.18,<4.0          # HuggingFace datasets (HotpotQA, etc.)

# ── Module III — GNN stack ────────────────────────────────────────────────────
torch-geometric>=2.5,<3.0
networkx>=3.3,<4.0

# ── Notebook runtime ──────────────────────────────────────────────────────────
jupyterlab>=4.1,<5.0
ipywidgets>=8.1,<9.0

# ── Module IV (no new packages — builds on II + III) ─────────────────────────
